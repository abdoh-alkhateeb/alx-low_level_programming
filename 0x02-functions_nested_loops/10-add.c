#include "main.h"

/**
 * add - a function that adds two integers and returns the result.
 * @a: first operand.
 * @b: second operand.
 *
 * Return: sum of a and b.
 */
int add(int a, int b)
{
	return (a + b);
}
