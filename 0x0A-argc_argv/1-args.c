#include <stdio.h>

/**
 * main - entry point of program.
 * @argc: argument count.
 * @argv: argument vector.
 *
 * Return: 0 (always) success.
 */
int main(int argc, char *argv[])
{
	argv = argv;

	printf("%d\n", argc - 1);

	return (0);
}
